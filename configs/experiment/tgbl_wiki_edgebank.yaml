defaults:
  - override /data: tgbl_wiki
  - override /model: edgebank
  - override /callbacks: default
  - override /trainer: default

logger:
  wandb:
    tags: ${tags}
    group: tgbl-wiki-edgebank

task_name: "eval"

tags: ["dev"]

ckpt_path: null
