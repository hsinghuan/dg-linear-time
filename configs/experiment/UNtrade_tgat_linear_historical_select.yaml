# @package _global_

defaults:
  - override /data: UNtrade
  - override /model: tgat
  - override /callbacks: default
  - override /trainer: gpu

logger:
  wandb:
    tags: ${tags}
    group: UNtrade-tgat-linear-historical-select

tags: ["dev"]

seed: 1

callbacks:
  early_stopping:
    monitor: val/historical/ap
    mode: max
    patience: 20
  model_checkpoint:
    monitor: val/historical/ap
    mode: max

trainer:
  min_epochs: 10
  max_epochs: 100
  devices: [1]
  num_sanity_val_steps: 0

model:
  sample_neighbor_strategy: uniform
  dropout: 0.1
  time_encoding_method: linear
  scale_timediff: true

data:
  eval_negative_sample_strategy: historical
